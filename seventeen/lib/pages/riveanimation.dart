import 'package:flutter/material.dart';

class RiveAnimationPrac extends StatefulWidget {
  @override
  _RiveAnimationPracState createState() => _RiveAnimationPracState();
}

class _RiveAnimationPracState extends State<RiveAnimationPrac> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Rive"),
        centerTitle: true,
      ),
      body: Text('sdggs'),
    );
  }
}
